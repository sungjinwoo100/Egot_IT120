import 'package:flutter/material.dart';

void main() {
  runApp(
    MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Scaffold(
        appBar: AppBar(
          title: Text('SizedBox'),
          centerTitle: true,
          backgroundColor: Colors.green,
          foregroundColor: Colors.white,
        ), //AppBar
        backgroundColor: Colors.grey.shade300,
        body: Center(
          child: SizedBox(
            width: 250,
            height: 250,
            child: Container(
              color: Colors.red, // At this stage of the code, the layout contains a parent SizedBox that defines the outer size, and multiple nested SizedBox widgets inside it, each wrapped in a Container with a different background color. The nested boxes are arranged from the largest to the smallest, creating layered, colorful squares where each inner SizedBox is centered inside its parent.
              child: Center(
                child: SizedBox(
                  width: 200,
                  height: 200,
                  child: Container(
                    color: Colors.green, // 2nd layer container child Sized box color green
                    child: Center(
                      child: SizedBox(
                        width: 150,
                        height: 150,
                        child: Container(
                          color: Colors.blue, // 3rd layer container child Sized box color blue
                          child: Center(
                            child: SizedBox(
                              width: 100,
                              height: 100,
                              child: Container(
                                color: Colors.yellow, // 4th layer container child sized box color yellow
                                child: Center(
                                  child: Text(
                                    '4 Layers',
                                    style: TextStyle(
                                      color: Colors.black,
                                      fontSize: 18,
                                    ),
                                  ),
                                ),
                              ),
                            ),
                          ),
                        ),
                      ),
                    ),
                  ),
                ),
              ),
            ),
          ),
        ),
      ),
    ),
  );
}
